server:
  port: 8080

spring:
  application:
    name: Demo Application for Azure Key Vault

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true